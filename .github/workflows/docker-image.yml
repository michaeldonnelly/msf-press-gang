name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Where the heck are we?
      run: pwd & ls
    - name: Build the Docker image
      run: cd Docker & ./build.sh
